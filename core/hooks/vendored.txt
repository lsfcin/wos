# Third-party files we did not author, exempt from every authoring rule for the reason
# core/hooks/SPECS.md § Generated artifacts gives.
#
# Same contract as gitignore-exceptions.txt: a NAMED, reviewed list, never a heuristic.
# Adding a line here is a claim that the file comes from upstream and is not ours to split.
# fnmatch globs, workspace-relative.

# ACM conference class templates (sigconf family), shipped with acmart.
academy/papers/*/sigconf*.tex
academy/papers/*/lib/sigconf*.tex

# ICLR style bundle.
academy/papers/*/iclr*_conference.tex
academy/papers/*/math_commands.tex

# Depth-Anything-V2 upstream checkpoint code, vendored for code/corpora.
code/corpora/depth_anything_v2/*
code/corpora/metric_depth/*

# Unzipped OOXML template — generated by extraction, not authored.
academy/administration/pda/template_extracted/*

# Drafts written BY another harness, kept side by side to be compared. Reflowing one to our column
# cap edits the artifact under study, and cutting it destroys the comparison — the two things the
# authoring rules would otherwise ask for. They are promoted into whatever owns them and then
# deleted, so the exemption expires with the directory. CONTEXT.md is ours and is not exempt.
brain/drafts/metodologia-*.md
brain/drafts/context-folding-report.md

# The BCC grade as SIGAA served it, saved from the browser — it carries its own
# `saved from url=` banner. A first-line comment of ours would be a claim we wrote it.
academy/administration/coordenacao-lc/novo-ppc-bcc/Nova Grade*.html
