# One "<domain>/<dir>" per line: a CONTEXT.md-bearing subdir Lucas deliberately wants left
# out of the .gitignore allowlist (reviewed, not an oversight). gitignore-self-heal.sh skips
# any name listed here instead of re-adding its `!<domain>/<dir>/` line.
